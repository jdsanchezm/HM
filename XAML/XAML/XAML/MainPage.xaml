<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XAML"
             x:Class="XAML.MainPage">

    <StackLayout BackgroundColor="Gray" Margin="30,50" Padding="10,20">
       
      <!--  <Grid BackgroundColor="Gray">
            <Grid.RowDefinitions>
                <RowDefinition Height="15"/>
                <RowDefinition Height="50%"/>
                <RowDefinition Height="15%"/>

            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        </Grid>-->
        <Label Text="Hola Mundo!" 
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand"/>
        <!-- Grid.Column="1" Grid.Row="1"-->
        <Button Text="Opción"  FontSize="Small"></Button>
        <!-- Grid.Column="1" Grid.Row="2"-->
       <!-- <Image Source="icon.png"></Image> Grid.Column="2" Grid.Row="0" -->
    </StackLayout>

</ContentPage>
