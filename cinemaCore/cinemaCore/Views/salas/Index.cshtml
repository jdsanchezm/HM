@model IEnumerable<cinemaCore.Models.sala>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ingreso)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ingreso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.noSala">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.noSala">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.noSala">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
