@model IEnumerable<CinemaMVC.Models.Sala>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ingreso)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ingreso)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.noSala }) |
            @Html.ActionLink("Details", "Details", new { id=item.noSala }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.noSala })
        </td>
    </tr>
}

</table>
